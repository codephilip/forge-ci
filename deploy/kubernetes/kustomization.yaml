# kubectl apply -k deploy/kubernetes   (after creating the forge-secrets Secret — see secret.example.yaml)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forge
resources:
  - namespace.yaml
  - forge.yaml
images:
  - name: ghcr.io/codephilip/forge-ci
    newTag: latest
